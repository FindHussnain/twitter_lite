<%= form_for([@article, @comment], :html => {class: "form-horizontal", role: "form"}) do |f| %>
  <%= f.text_area :body, class: "form-control", placeholder: "Add a Comment", autofocus: true %>
  <%= f.submit class: 'btn btn-success btn-xs' %>
<% end %>
